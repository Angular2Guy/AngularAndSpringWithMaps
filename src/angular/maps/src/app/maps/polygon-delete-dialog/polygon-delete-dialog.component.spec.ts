import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PolygonDeleteDialogComponent } from './polygon-delete-dialog.component';

/*describe('PolygonDeleteDialogComponent', () => {
  let component: PolygonDeleteDialogComponent;
  let fixture: ComponentFixture<PolygonDeleteDialogComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PolygonDeleteDialogComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(PolygonDeleteDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
